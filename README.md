# For installing dependencies:


python -m venv .venv
source .venv/bin/activate   # Linux / macOS

sau

.venv\Scripts\activate      # Windows

## uv pip install streamlit groq instructor pydantic beautifulsoup4 requests python-dotenv




# For saving dependencies with versions:

pip freeze > requirements.txt
